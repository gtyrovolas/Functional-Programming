




Claim: If f, g strict, then f.g strict
Proof:
f( g ⊥  )  = f ( ⊥  ) since g strict
	   = ⊥   

if f . g strict then f and g are not neccessarily strict
for instance g x = True : x, f x = ⊥ 


f needs to be strict because


 
> f x = (True, x)
> g x = snd x










